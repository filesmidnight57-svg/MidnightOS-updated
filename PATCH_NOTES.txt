MidnightOS Verified Patch

Fixed:
- Direct dry-run exits before AI generation, rendering, publishing, or case creation.
- Runtime JSON uses explicit UTF-8 and repairs common Hindi mojibake on read.
- JSON state writes are atomic (temporary file, fsync, rename).
- Missing, empty, or corrupt JSON state recovers safely and preserves a backup.
- Project-local persisted paths are stored as portable relative paths.
- Unsafe relative path traversal is rejected.
- cases.json uses the shared safe state writer/recovery logic.
- Runtime state files and temporary/corrupt backups are ignored by Git.
- npm run check validates every JavaScript file under src and test.
- Accidental terminal-created filename removed.

Verification completed:
- npm test: 8/8 passed
- npm run check: passed
- npm run morning -- --count=1 --dry-run: passed

Install:
1. Keep your old .env file safe.
2. Extract this ZIP to a new folder.
3. Copy your old .env into the extracted MidnightOS folder.
4. Run npm install.
5. Run npm test.
6. Run npm run check.
7. Run npm run morning -- --count=1 --dry-run.
